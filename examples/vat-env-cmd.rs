use vat::repository::VatRepository2 as VatRepository;
fn main(){
    let command = "dcc houdini20";
    // let result = VatRepository::run_command(&command, None);
    // println!("result: {:?}", result);
}
